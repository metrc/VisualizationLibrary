% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/closed_tables.R
\name{closed_survival_analysis_bayes_poisson}
\alias{closed_survival_analysis_bayes_poisson}
\title{Closed Survival Analysis Bayesian Poisson Noninferiority}
\usage{
closed_survival_analysis_bayes_poisson(
  analytic,
  type_construct,
  days_construct,
  outcome_length = 365,
  cuts = c(0, 90, 180, 270, 365),
  ni_margin = 0.1,
  control_arm = "Group A",
  baseline_prior_mean = -8.15,
  baseline_prior_sd = 1.5,
  treatment_prior_sd = 1,
  arm_labels = c(`0` = "Control", `1` = "Treatment"),
  outcome_label = "Outcome",
  chains = 4,
  iter = 4000,
  warmup = 2000,
  cores = 4,
  seed = 20260713,
  adapt_delta = 0.95,
  backend = "cmdstanr",
  install_cmdstan = TRUE,
  blinded = FALSE
)
}
\arguments{
\item{analytic}{This is the analytic dataset that must include enrolled, treatment_arm, and study_id}

\item{type_construct}{the name of the column of the analytic dataset that must include whether the outcome for that participant was a check or event}

\item{days_construct}{the name of the column of the analytic dataset that must include the number of days till check or event}

\item{outcome_length}{number of days for this outcome (defaults to 365)}

\item{cuts}{interval boundaries for the piecewise baseline hazard; must start at 0 and end at outcome_length.
These should be selected and locked before unmasking (defaults to quarterly intervals c(0, 90, 180, 270, 365))}

\item{ni_margin}{noninferiority margin for the risk difference (defaults to 0.10)}

\item{control_arm}{value of treatment_arm treated as the control group (defaults to "Group A")}

\item{baseline_prior_mean}{prior mean for each log baseline-hazard interval coefficient (defaults to -8.15, near a 10 percent annual event risk)}

\item{baseline_prior_sd}{prior standard deviation for each interval coefficient (defaults to 1.5, intentionally broad)}

\item{treatment_prior_sd}{prior standard deviation for the treatment log hazard ratio, centered at no effect (defaults to 1)}

\item{arm_labels}{named chr vec, c("0" = "Control", "1" = "Treatment")}

\item{outcome_label}{label for the outcome row of the table}

\item{chains}{number of MCMC chains (defaults to 4)}

\item{iter}{total iterations per chain (defaults to 4000)}

\item{warmup}{warmup iterations per chain (defaults to 2000)}

\item{cores}{number of cores for sampling (defaults to 4)}

\item{seed}{random seed for sampling (defaults to 20260713)}

\item{adapt_delta}{target acceptance rate passed to the sampler (defaults to 0.95)}

\item{backend}{brms backend, "cmdstanr" or "rstan" (defaults to "cmdstanr")}

\item{install_cmdstan}{when TRUE and backend = "cmdstanr", installs CmdStan version 2.35.0
into the working directory (quietly, overwriting any existing installation) before fitting.
Set to FALSE to reuse an existing CmdStan installation (defaults to TRUE)}

\item{blinded}{when TRUE, ignores the real treatment_arm and deterministically reassigns
arms from the digit sum of study_id (even = "Group A", odd = "Group B") so the table can be
produced without unmasking (defaults to FALSE)}
}
\value{
An HTML table.
}
\description{
This function divides the time to event (event or time to last observation) into
prespecified intervals and performs a Bayesian Poisson regression (the Poisson representation of a
piecewise-exponential proportional-hazards model) to estimate the hazard of an event through
outcome_length days. Hazards are converted to proportions, and the group difference with its 95
percent credible interval is used to apply a noninferiority decision rule.

Requires the brms package (and the cmdstanr package when backend = "cmdstanr"; by default
CmdStan itself is installed into the working directory before fitting via install_cmdstan).
All installation, compilation, and sampling output is suppressed; the only output is the
returned HTML table.
}
\examples{
\dontrun{
closed_survival_analysis_bayes_poisson("Replace with Analytic Tibble",
                                       type_construct = "surgery_or_healed_type",
                                       days_construct = "surgery_or_healed_days")
}
}
